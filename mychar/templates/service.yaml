apiVersion: v1
kind: Service
metadata:
  name: {{ include "mychar.fullname" . }}
  labels:
    {{- include "mychar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mychar.selectorLabels" . | nindent 4 }}
